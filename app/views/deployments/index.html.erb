<% content_for :page_title, @application.name %>

<%= render partial: "shared/application_header", locals: { application: @application, tab: "deploys" } %>

<%= render "deployments_list", deploys: @deployments %>
