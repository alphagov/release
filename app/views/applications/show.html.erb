<% content_for :page_title, @application.name %>

<h1><%= @application.name %></h1>

<dl>
  <dt>Repo:</dt>
  <dd><%= link_to @application.repo, @application.repo %></dd>
</dl>

<div class="btn-group">
  <%= link_to "Edit", { action: "edit", id: @application.id }, class: "btn" %>
  <%= link_to "Create Deploy Task", new_task_path, class: "btn" %>
</div>
