<% content_for :page_title, "Applications" %>

<h1>Applications</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th>Repo</th>
    </tr>
  </thead>
  <tbody>
  <% @applications.each do |application| %>
    <tr>
      <td><%= link_to application.name, application %></td>
      <td><%= link_to application.repo, application.repo %></td>
    </tr>
  <% end %>
  </tbody>
</table>
