<% content_for :page_title, "Create new Application" %>

<%= render "govuk_publishing_components/components/breadcrumbs", {
  breadcrumbs: [
    root_crumb,
    {
      title: "Create new application"
    }
  ]
} %>

<%= render "shared/form_errors", resource: @application %>

<%= render "govuk_publishing_components/components/heading", {
  text: "Create new application",
  font_size: "xl",
  heading_level: 1,
  margin_bottom: 8
} %>

<%= render "govuk_publishing_components/components/lead_paragraph", {
  text: "Applications are created automatically when first deployed, but you can create them manually if you like."
} %>

<%= render partial: "form", locals: { application: @application } %>
